#!/usr/bin/env python3
"""
Benchling Webhook Configuration CLI Entry Point

Wrapper script for the XDG configuration CLI.
This script can be called directly or via npm scripts.
"""

import sys
from pathlib import Path

# Add src to path
sys.path.insert(0, str(Path(__file__).parent.parent / "src"))

from src.xdg_cli import cli

if __name__ == "__main__":
    cli()
