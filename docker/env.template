# ==============================================================================
# REQUIRED USER VALUES (Set these BEFORE running aws/setup.sh)
# ==============================================================================

# Quilt Configuration
S3_BUCKET_NAME=your-data-bucket
QUILT_CATALOG=your-catalog.quiltdata.com
QUILT_DATABASE=your-athena-database
SQS_QUEUE_URL=https://sqs.us-east-2.amazonaws.com/123456789012/PackagerQueue

# Benchling Configuration
BENCHLING_TENANT=your-tenant (e.g. `mycompany` from `mycompany.benchling.com`)
BENCHLING_CLIENT_ID=your-client-id
BENCHLING_CLIENT_SECRET=your-client-secret

BENCHLING_APP_DEFINITION_ID=appdef_your_id_here  # REQUIRED when ENABLE_WEBHOOK_VERIFICATION=true. Get from Benchling app settings (e.g., appdef_xxxx)
ENABLE_WEBHOOK_VERIFICATION=true  # Set to false ONLY for local development. Production MUST have this enabled with valid BENCHLING_APP_DEFINITION_ID

BENCHLING_TEST_ENTRY=etr_123456789 # only need to run tests

# ==============================================================================
# OPTIONAL VALUES (Defaults provided, override if needed)
# ==============================================================================

# Flask Configuration
FLASK_ENV=production  # Options: development (human-readable logs), production (JSON logs)
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# AWS Configuration
AWS_REGION=us-east-2

# Webhook Configuration
PKG_PREFIX=benchling
PKG_KEY=experiment_id
PKG_BUCKET_ONLY=false  # Set to true to restrict searches to only the configured bucket

# Ngrok Configuration (optional - for local development)
# Get your authtoken from: https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN=your_ngrok_authtoken_here
NGROK_DOMAIN=your-subdomain.ngrok-free.app # Optional: set your custom ngrok domain
