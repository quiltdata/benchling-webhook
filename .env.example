# Benchling Webhook Environment Variables (v1.0.0+)
#
# This file shows all required environment variables for local development.
# Copy to `.env` and fill in your values:
#   cp .env.example .env
#
# For production deployments, these are managed by the CDK deployment
# and stored in AWS Secrets Manager and CloudFormation parameters.

# =============================================================================
# AWS Configuration
# =============================================================================

# AWS region for your deployment
AWS_REGION=us-east-1

# =============================================================================
# Quilt Service Configuration (v1.0.0+)
# =============================================================================

# SQS queue URL for Quilt package creation jobs
# Example: https://sqs.us-east-1.amazonaws.com/123456789012/quilt-packager-queue
PACKAGER_SQS_URL=

# Athena/Glue database name for Quilt catalog metadata
# Example: quilt_catalog
ATHENA_USER_DATABASE=

# Quilt catalog domain (without protocol or trailing slash)
# Example: quilt.example.com
QUILT_WEB_HOST=

# Iceberg database name (optional, leave empty if not used)
# Example: quilt_iceberg
ICEBERG_DATABASE=

# =============================================================================
# Benchling Configuration
# =============================================================================

# Benchling tenant ID (from your Benchling URL)
# Example: your-company (from https://your-company.benchling.com)
BENCHLING_TENANT=

# Benchling OAuth Client ID (from app settings)
BENCHLING_CLIENT_ID=

# Benchling OAuth Client Secret (from app settings)
# WARNING: Keep this secret! Never commit to version control.
BENCHLING_CLIENT_SECRET=

# Benchling App Definition ID (from app manifest)
BENCHLING_APP_DEFINITION_ID=

# =============================================================================
# Package Storage Configuration
# =============================================================================

# S3 bucket for Benchling packages
# Example: my-benchling-packages
BENCHLING_PKG_BUCKET=

# S3 key prefix for packages
# Default: benchling
BENCHLING_PKG_PREFIX=benchling

# Metadata key for package identification
# Default: experiment_id
BENCHLING_PKG_KEY=experiment_id

# =============================================================================
# Application Configuration
# =============================================================================

# Flask environment (development or production)
FLASK_ENV=development

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Enable webhook signature verification (true or false)
# Set to false for local testing, true for production
ENABLE_WEBHOOK_VERIFICATION=true

# =============================================================================
# Deployment (Managed by CDK, not needed for local development)
# =============================================================================

# AWS Secrets Manager ARN for Benchling credentials (production only)
# BENCHLING_SECRET_ARN=arn:aws:secretsmanager:us-east-1:123456789012:secret:benchling-webhook-credentials

# Benchling webhook version (set automatically by deployment)
# BENCHLING_WEBHOOK_VERSION=1.0.0

# =============================================================================
# Notes
# =============================================================================
#
# For local development:
#   1. Copy this file to .env
#   2. Fill in all required values
#   3. Run: docker compose up app-dev
#
# For production deployment:
#   1. Run setup wizard: npm run setup
#   2. Deploy: npm run deploy
#   3. Values are automatically managed by AWS
#
# Migration from v0.8.x:
#   - Old variable names (QUEUE_URL, QUILT_CATALOG, etc.) are replaced
#   - See MIGRATION.md for details
#   - QuiltStackARN is no longer used at runtime
